server:
  port: 8087
spring:
  data:
    mongodb:
      uri: mongodb://localhost/spring-cloud
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  security:
    enabled: false